node_modules/
dist/
.next/
.turbo/
coverage/
.cache/
*.log
.env
.env.bak
.env.*.local
!.env.example
.DS_Store
.idea/
.vscode/
*.tsbuildinfo
.pyc
__pycache__/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
storage/
artifacts/
prisma/migrations/*/migration_lock.toml.bak.checkpoints/

# Archivos subidos por los tenants (logo). Servidos por el API en /uploads.
apps/commerce-api/uploads/

# Cache local de pnpm y salida del análisis de graphify (no son parte del producto).
.pnpm-store/
graphify-out/

# Configuración del agente por tenant (runtime, no código): igual que uploads/,
# vive en disco local y no se versiona.
apps/agent-v2/.settings/

# Artefactos del MCP de Playwright (screenshots, snapshots) durante pruebas E2E.
.playwright-mcp/

# Capturas de la conversación tomadas para revisión de UI.
*.jpeg
!/docs/**
!/apps/web/public/**
.vercel

# Screenshots de testing/revisión (Playwright MCP, revisión de UI manual)
# no deben subirse al repo. Solo bloquea las del root, NO las de assets.
*-placeholders.png
login-*.png
recover-*.png
super-admin-*.png
customers-*.png
channels-*.png
/*.png
